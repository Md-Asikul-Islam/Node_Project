
const ageCal = (name, age) => {
    return` Hi ${name}, you are ${age} years old `
}

const name = " Asikul Islam "

const fruits = ['Apple', 'Banana', 'Mango', 'Orange', 'Pineapple', 'Strawberry', ]

module.exports = ageCal;